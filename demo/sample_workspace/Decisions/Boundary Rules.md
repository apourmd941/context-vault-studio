# Boundary Rules

The generated vault is the actual AI boundary.

- Prefer `copy` mode when you want a hard curated corpus.
- Use `symlink` mode only when you want live editing convenience.
- Keep sensitive material in blocked paths or outside allowed roots.
